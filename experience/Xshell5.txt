账号切换：密码 账号 等同
su - anhao-recipe-h5-static
1、进入static的目录下
cd src
cd static
2、删除dist文件
rm -rf dist
3、上传dist的zip压缩包/解压
rz
unzip dist.zip
4、删除dist的压缩包
rm -rf dist.zip

5、开启npm目录操作权限
sudo chown -R $(whoami) /home/wedata-static/htdocs/business-front/union-platform-static
备注：后面是需要开启的目录，从根节点开始；
临时权限的话，可以在命令行前加sudo;例如：sudo npm run build
1、seesLabels 对应 标注；resultLabels 对应 结果

2、影像工具渲染流程：先渲染dom，在渲染标注。

3、nw新开窗口原理：
相应的页面，配置好，监听隐藏显示的指令。每一个页面就是一个实例tabinteraction。

4、再次打开登录页原理：
其实就是新开weiyiList页面，然后重定向至登录页；
this.tabinteraction.deltList();==删除weiyiList记录，保证每次可以新开页面；
this.tabinteraction.createTab('weiyiList');==新开一个页面list(该页面就是重定向到登录的页面)

=======服务器==========
root权限 root/Gh^_^1610

服务器 192.168.3.144 wedata-static/wedata-static

服务器 192.168.94.44 ai-static/ai-static

nginx配置 端口 对应 静态文件目录（测试/开发环境）

原因：包替换错误以及测试环境缺少文件；
解决：相应服务器下的静态文件替换即可；或者重新打个包即可；


======打包/部署=========
1、压缩文件zip
2、dist_client目录
3、打包exe文件(官网）
1）命令行：copy /b nw.exe + package.nw acv.exe
其实改不改zip都没影响，官网推荐这样做罢了；
2）icon图标，直接用工具替换一下即可；Resource Hacker替换图标；IconWorkshop图片转图标；
4、配置
1）前置机IP（后台提供）
2）端口/以及代理等等保持不变即可（代理-登录微医云外网用的）
5、文件监听规则
1）新增患者需要注意
2）特殊处理的点注意

一、瓯海落地部署
1、远程 1036635106		eat922
2、新增患者逻辑和修改患者逻辑有点问题；有outSno不应该再修改才对；(推送)

二、落地医院问题汇总：
1、404 
nginx配置问题
2、客户端启动不了，打不开了没反应
反向代理配置问题
3、压缩包上传不了
文件名不要带中文
初始化：
全局实例化：传入相应的数据；表头、表体、搜索
组件实例化：
    1、表头
    过滤器：过滤表头的数据；
    样式：选中状态、排序状态；
    事件：绑定排序方法；
    2、表体
    计算属性：生成相应的表格内容；


更新（交互）：
    1、排序
    事件触发——data改变——更新DOM：过滤器、计算属性；

    2、search
    searchQuery值改变（双向绑定）——更新DOM：过滤器、计算属性；
